# Describes the runners that the CI system can use

ubuntu-24.04:
  arch: x86_64
  platform: linux
  free: true

ubuntu-24.04-arm:
  arch: aarch64
  platform: linux
  free: true

macos-26:
  arch: aarch64
  platform: darwin
  free: true

windows-2025-vs2026:
  arch: x86_64
  platform: windows
  free: true

ubuntu-latest:
  arch: x86_64
  platform: linux
  free: true

macos-latest:
  arch: aarch64
  platform: darwin
  free: true

windows-latest-large:
  arch: x86_64
  platform: windows
  free: false

windows-latest:
  arch: x86_64
  platform: windows
  free: true

windows-11-arm:
  arch: aarch64
  platform: windows
  free: false

blacksmith-2vcpu-ubuntu-2404:
  arch: x86_64
  platform: linux
  free: false
